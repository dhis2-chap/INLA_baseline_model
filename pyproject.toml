[project]
name = "inla-baseline-model"
version = "1.0.0"
description = "INLA Baseline Model with chapkit integration"
requires-python = ">=3.13"
dependencies = [
    "chapkit",
    "fastapi[standard]>=0.119.1",
    "uvicorn[standard]>=0.30.0",
    "gunicorn>=23.0.0",
]

[tool.uv.sources]
chapkit = { git = "https://github.com/dhis2-chap/chapkit.git", branch = "main" }
